# lambda function: A function written in one line using the lambda keyword.abs
#                  lambda <parameters> : <expressioon>

# Define lambda function
multiply = lambda x, y : x * y

# Call lambda function
print(multiply(3, 4))